spring:
  datasource:
    driver-class-name: "org.h2.Driver"
    url: "jdbc:h2:mem:db;DB_CLOSE_DELAY=-1"
    username: "sa"
    password: "sa"
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true


sd-creation-wizard-api:
  base-url: test

daps-server:
  url:
    internal: test
    external: test

did-web-service:
  base-url: test
  ignore-ssl: true

fh:
  catalog:
    url: url
    secret-key: secret